// ============================================================
// DASHBOARD FINANZAS - Centro Financiero
// Este módulo NO envuelve render(). Solo expone sincronización.
// ============================================================
(function (global) {
    'use strict';
    function getFinancialSummary() { return typeof getResumenPagos==='function' ? getResumenPagos() : {totalFacturado:0,totalCobrado:0,totalPorCobrar:0,deudasPendientes:0,deudasParciales:0,deudasPagadas:0}; }
    function getAcceptedDebtRows() {
        const cots=Array.isArray(S.cotizaciones)?S.cotizaciones:[];
        return cots.map(c=>({...c,...(typeof getResumenCotizacion==='function'?getResumenCotizacion(c):{montoTotal:cotTotal(c),totalPagado:0,saldoPendiente:cotTotal(c),porcentaje:0,pagos:[]})})).filter(c=>Number(c.montoTotal)>0&&Number(c.saldoPendiente)>.01).sort((a,b)=>Number(b.saldoPendiente)-Number(a.saldoPendiente));
    }
    function getPaymentTransactions() {
        const out=[];(S.cotizaciones||[]).forEach(c=>{const principal=typeof getPagoPrincipalByCotizacionId==='function'?getPagoPrincipalByCotizacionId(c.id):null;const regs=typeof getPagosRegistradosByCotizacionId==='function'?getPagosRegistradosByCotizacionId(c.id):[];if(regs.length)regs.forEach(p=>{if(Number(p.montoPagado||0)>0)out.push({...p,montoPagado:Number(p.montoPagado||0),cotizacion:c});});else if(principal&&Number(principal.montoPagado||0)>0)out.push({...principal,montoPagado:Number(principal.montoPagado||0),cotizacion:c});});return out.filter(p=>p.fecha).sort((a,b)=>String(b.fecha).localeCompare(String(a.fecha)));
    }
    function buildPanelContent(items) {
        if(!items.length)return `<div class="empty" style="padding:20px;">${ICONS.empty}<div>🎉 No hay pagos pendientes por cobrar.</div></div>`;
        return items.map(item=>{const pagos=typeof getPagosOrdenados==='function'?getPagosOrdenados(getPagosRegistradosByCotizacionId(item.id)):[];const ultimo=pagos[0];const pct=Math.min(100,Math.max(0,Number(item.porcentaje)||0));return `<div style="margin-bottom:20px;padding:16px 18px;border:1px solid var(--border);border-radius:var(--radius);background:var(--surface);"><div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:12px;"><div style="flex:1;min-width:200px;"><div style="font-weight:600;font-size:15px;color:var(--primary);">${esc(item.titulo||'')}</div><div style="font-size:12px;color:var(--text-soft);">${esc(item.cliente||'')} - ${esc(item.proyecto||'')}</div>${ultimo?`<div style="font-size:12px;color:var(--text-soft);margin-top:4px;">${fmtDate(ultimo.fecha)} - ${esc(ultimo.descripcion||'')}</div>`:''}</div><div style="text-align:right;min-width:120px;"><div style="font-weight:600;font-size:16px;color:var(--primary);">${bs(item.totalPagado)} / ${bs(item.montoTotal)}</div><div style="font-size:12px;color:var(--text-soft);">Saldo: <strong>${bs(item.saldoPendiente)}</strong></div></div></div><div style="margin-top:10px;height:6px;background:var(--gantt-bg);border-radius:4px;overflow:hidden;"><div style="height:100%;border-radius:4px;background:${pct>=80?'var(--gantt-active)':'var(--accent)'};width:${pct}%;"></div></div><div style="margin-top:12px;display:flex;gap:8px;flex-wrap:wrap;"><button class="btn btn-sm btn-success" onclick="openPagoFromCotizacion('${esc(item.id)}')">${ICONS.plus} Registrar pago</button><button class="btn btn-sm btn-ghost" onclick="S.view='finanzas';render();">Gestionar pagos</button></div>${pagos.length?`<div class="payment-history" style="margin-top:10px;">${pagos.slice(0,3).map(p=>`<div class="entry"><span>${fmtDate(p.fecha)} - ${esc(p.descripcion||'')}${p.metodoPago?` <span class="metodo-pago-badge ${p.metodoPago}">${metodoPagoLabel(p.metodoPago)}</span>`:''}</span><span style="font-weight:500;">${bs(p.montoPagado)}</span></div>`).join('')}${pagos.length>3?`<div class="entry" style="color:var(--text-soft);font-style:italic;">...y ${pagos.length-3} pagos más</div>`:''}</div>`:''}</div>`;}).join('');
    }
    function buildFinancialCenter() {
        const s=getFinancialSummary(),cartera=getAcceptedDebtRows(),tx=getPaymentTransactions(),months=[],now=new Date();for(let i=5;i>=0;i--){const d=new Date(now.getFullYear(),now.getMonth()-i,1);months.push({key:`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,label:d.toLocaleDateString('es-BO',{month:'short'}),total:0});}tx.forEach(p=>{const m=months.find(x=>x.key===String(p.fecha).slice(0,7));if(m)m.total+=Number(p.montoPagado||0);});const max=Math.max(1,...months.map(m=>m.total));const k=(i,l,v,n)=>`<div style="padding:14px;border:1px solid var(--border);border-radius:var(--radius);background:var(--surface);"><div style="font-size:11px;color:var(--text-soft);text-transform:uppercase;">${i} ${l}</div><div style="font-size:20px;font-weight:650;color:var(--primary);margin-top:5px;">${v}</div><div style="font-size:11px;color:var(--text-soft);">${n}</div></div>`;const ch=cartera.length?cartera.slice(0,8).map(c=>`<div style="display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:10px 0;border-bottom:1px solid var(--border);"><div><div style="font-size:13px;font-weight:550;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${esc(c.titulo||'Sin título')}</div><div style="font-size:11px;color:var(--text-soft);">${esc(c.cliente||'')} · ${Number(c.porcentaje||0).toFixed(0)}% cobrado</div></div><div style="font-size:13px;font-weight:600;">${bs(c.saldoPendiente)}</div></div>`).join(''):'<div style="padding:16px 0;color:var(--text-soft);font-size:13px;">No hay cartera pendiente.</div>';
        return `<div class="panel" id="centro-financiero" style="margin-top:18px;"><div class="panel-h"><div><h3>💰 Centro Financiero</h3><span>Resumen de cartera y comportamiento de cobros</span></div><button class="btn btn-sm btn-ghost" onclick="S.view='finanzas';render();">Gestionar pagos</button></div><div class="panel-body"><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px;margin-bottom:18px;">${k('💵','Cobrado acumulado',bs(s.totalCobrado),`${tx.length} registros de cobro`)}${k('📥','Por cobrar',bs(s.totalPorCobrar),`${cartera.length} deudas pendientes`)}${k('⏳','Deudas pendientes',s.deudasPendientes,'Sin pagos registrados')}${k('🔄','Pagos parciales',s.deudasParciales,'Cotizaciones con saldo')}${k('✅','Pagos completados',s.deudasPagadas,'Sin saldo pendiente')}</div><div style="display:grid;grid-template-columns:minmax(0,1.15fr) minmax(280px,.85fr);gap:18px;"><div><div style="font-size:13px;font-weight:600;margin-bottom:6px;">📌 Cartera pendiente</div>${ch}${cartera.length>8?'<div style="font-size:11px;color:var(--text-soft);padding-top:8px;">Mostrando las 8 deudas con mayor saldo.</div>':''}</div><div><div style="font-size:13px;font-weight:600;margin-bottom:10px;">📈 Cobros por mes</div><div style="display:flex;align-items:flex-end;gap:8px;height:130px;">${months.map(m=>`<div style="flex:1;height:100%;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;gap:5px;"><div title="${m.label}: ${bs(m.total)}" style="width:100%;max-width:42px;height:${Math.max(3,m.total/max*100)}px;border-radius:4px 4px 0 0;background:var(--accent);"></div><span style="font-size:10px;color:var(--text-soft);text-transform:capitalize;">${m.label}</span></div>`).join('')}</div></div></div></div></div>`;
    }
    function syncDashboardFinancialView(){
        if(S.view!=='dashboard')return;const main=document.getElementById('main');if(!main)return;const heading=Array.from(main.querySelectorAll('.panel h3')).find(e=>(e.textContent||'').includes('Avance de pagos pendientes'));if(heading){const panel=heading.closest('.panel'),body=panel?.querySelector('.panel-body'),items=getAcceptedDebtRows();heading.textContent=`💰 Avance de pagos pendientes (${items.length})`;const note=heading.closest('.panel-h')?.querySelector('span');if(note)note.textContent=`${items.filter(p=>p.porcentaje>0).length} con pagos parciales`;if(body)body.innerHTML=buildPanelContent(items);}const existing=document.getElementById('centro-financiero');if(existing)existing.outerHTML=buildFinancialCenter();else{const panels=main.querySelectorAll('.panel'),target=Array.from(panels).find(p=>(p.textContent||'').includes('Avance de pagos pendientes'))||panels[panels.length-1];if(target)target.insertAdjacentHTML('afterend',buildFinancialCenter());}
    }
    global.syncDashboardFinancialView=syncDashboardFinancialView;global.getAcceptedDebtRows=getAcceptedDebtRows;
})(window);
